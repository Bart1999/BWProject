<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">BW Labels</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <!-- When Author is signed in this will be the menu-->
        <% if author_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Sign out', destroy_author_session_path, method: :delete, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Posts', posts_path, class: 'nav-link' %>
          </li>
          <ul class="navbar-nav">
            <!-- brings in search function -->
          <%= render 'search/form' %>
          </ul>
        <% else %>
        <!-- When user is not signed in they're a reader, this will be the menu-->
          <li class="nav-item">
            <%= link_to 'Register', new_author_registration_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign in', new_author_session_path, class: 'nav-link' %>
          </li>
          <ul class="navbar-nav">
            <!-- brings in search function -->
          <%= render 'search/form' %>
          </ul>
        <% end %>
      </ul>
    </div>
</nav>
